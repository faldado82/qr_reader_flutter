import 'package:flutter/material.dart';

class HistoryMapasPage extends StatelessWidget {
  const HistoryMapasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('History Mapas Page')
    );
  }
}